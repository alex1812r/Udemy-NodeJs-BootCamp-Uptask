<!DOCTYPE html>
html(lang="en")
  head
    include partials/head
  body.login
    .contenedor-formulario
      h1= nombrePagina
      p Ingresa nueva clave
              
      form.caja-login(method="POST" )
          .campo
            label(for="password") Password: 
            input(type="password" name="password" value=`${password || ''}` placeholder="Nuevo password")
          .campo.enviar
            input.boton(type="submit" value="Reestablecer clave")
