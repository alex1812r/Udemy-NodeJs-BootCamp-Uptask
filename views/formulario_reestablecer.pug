<!DOCTYPE html>
html(lang="en")
  head
    include partials/head
  body.login
    .contenedor-formulario
      h1= nombrePagina
      p Agraga tu Email para reestablecer la Clave

      if locals.mensajes
        .alertas
         each categoria in Object.keys(locals.mensajes)
          each error in mensajes[categoria]
            .alerta(class=`${categoria}`)
              span=error
              
      form.caja-login(method="POST" action="/reestablecer")
          .campo
            label(for="email") E-mail: 
            input(type="email" name="email" value=`${email || ''}` placeholder="Tu Email")
          .campo.enviar
            input.boton(type="submit" value="Reestablecer clave")
          .campo.acciones
            a(href="/iniciar_sesion") Volver al Login
